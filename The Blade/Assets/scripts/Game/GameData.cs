using UnityEngine;

public static class GameData
{
    //playtime
    public enum SceneList
    {
        MainMenu,
        BaseScene,
        persistentObjects,
    }

    public enum ActionMapList {
        Player,
        UI,
        Persistent,
    }
}
