using UnityEngine;

public static class GameData
{
    public enum SceneList
    {
        MainMenu,
        BaseScene,
        persistentObjects,

    }

    public enum ActionMapList {
        Player,
        UI,
        Persistent,
        
    }
}
